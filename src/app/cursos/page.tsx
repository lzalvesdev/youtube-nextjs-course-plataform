import { Metadata } from "next";

export const metadata: Metadata = {
  title: "EducaNet - Todos os cursos",
};

export default function PageCursos() {
  return(
    <main>
      cursos
    </main>
  );
}